services:
  web:
    build: .
    ports:
     - "8000:8000"
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000
    volumes:
     - .:/app
    environment:
      - "PLAYIT_TABLE_FILE_NAME=playIT-2024.xlsx"